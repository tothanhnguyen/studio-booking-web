{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack pnpm install --frozen-lockfile",
  "buildCommand": "corepack pnpm prisma generate && corepack pnpm check:deploy && corepack pnpm build",
  "regions": ["sin1"]
}
